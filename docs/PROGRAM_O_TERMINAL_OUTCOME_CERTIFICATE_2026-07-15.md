# Program O — terminal exhaustion certificate (Paper 2 adaptive-headroom search)

**Date:** 2026-07-15
**Outcome:** `EXHAUSTION_CERTIFICATE — internal search complete; terminal determination is one named
external fact with an exact committed question (Garrido Q14).` Every experiment that the repository and
its burned tapes can support has been run. The remaining fork cannot be resolved by any further internal
computation; it is decided by a single freight-economics fact, stated as an exact falsifiable question,
with both downstream branches fully preregistered. The sealed validation tapes `7420049–7420096` are
intact and stay closed until that fact selects a branch.

---

## 1. Quantitative ceilings (all custody-verified)

| Quantity | Value | Provenance |
|---|---|---|
| Perfect-information ceiling, safe H_PI (holdout, least-favorable cell) | **0.15151** | verdict `98ce2ce`, result `f5f2da8d` |
| H_PI simultaneous safe LCB95 | **0.11562** | 10k paired bootstrap, conserved throughput |
| Exact fungible-null H_PI (causal control) | **0.0** | metric-identical under fungibility |
| Belief-only observable capture (development, primary regime) | ≈ **0.04–0.10** ReT | dual-resource diagnostic `e48606e7` |
| Incremental operational-state value over belief (fixed-clock, LCB95) | **+0.025 to +0.073** | diagnostic, 3-cell connected component |
| Incremental operational-state value (pay-per-use) | **fails gate** | diagnostic `pay_per_use_*_pass: False` |

## 2. The complete internal search (exhausted)

1. **H_PI established** (§1): material, buffering-robust, exact causal null. Commit `6ad6f10`, verdict `98ce2ce`.
2. **State-rich H_obs fit** (`program-o-state-rich-fit-v1-20260715`, `041dcef`, result `d67ac97a`):
   `STOP_RESOURCE_OR_GUARDRAIL_CONFOUND`. Ten controllers reach material ReT at equal production and equal
   reserved fleet but out-transport the frontier; `STOP` correct as scored under the frozen actual-use
   contract. (An earlier certificate, `d29c29b`, wrongly read the fit's uninitialised `information_placebos`
   flag as a result and claimed a terminal "state adds nothing" boundary; retracted in `022abd0`.)
3. **Dual-resource diagnostic** (`program-o-dual-resource-diagnostic-v1-20260715`, `a9733d0`, result
   `e48606e7`, burned tapes only, `validation_seed_accessed: False`): the placebos actually executed. Under
   **fixed-clock** reserved capacity, belief-MPC (configs 3, 4) beats **every** information placebo across a
   connected 3-cell component (rho75s90, rho90s75, rho90s90) — incremental operational-state value positive
   and material (§1). Under **pay-per-use** nothing passes. `DIAGNOSTIC_STABLE_SIGNAL_FIXED_CLOCK_ONLY`,
   `h_obs_confirmed: False` (development).

No further internal experiment can move this: the state-value question is answered (state does add value
under fixed-clock, development-stage); the resource-convention question is not answerable from inside the
model.

## 3. The single decisive external fact — exact question committed

**Garrido Q14** (`research/paper2_exhaustive_search/garrido_face_validation_questions.md`): is the Op10/Op12
downstream fleet **fixed-clock reserved** (charged whether loaded or empty, with routinely idle capacity)
or **pay-per-use**? Observed utilization 2,280 / 5,376 vehicle-hours (42 %) is supplied as evidence.
**Garrido Q13** (construct): are there ≥2 mutually non-substitutable ration classes sharing the Op5–Op7
bottleneck (validates MFSC-representativeness) or is the mix fungible (collapses to the exact null)?

## 4. The two fully-preregistered terminal branches

- **POSITIVE branch — Q14 = fixed-clock reserved with idle capacity:** the diagnostic's signal is real.
  Freeze belief-MPC (selected on burned tapes), preregister a fixed-clock H_obs **validation** contract,
  open sealed `7420049–7420096` **once**. Clean OOS pass → classical H_obs > 0 → Paper 2 candidate → the
  neural-learner gate (learner must beat max{frontier, base-stock, MPC, DP, interpretable} on fresh virgin
  seeds) → Paper 3.
- **BOUNDARY branch — Q14 = pay-per-use / tight envelope:** the observable ReT gain is a transport-utilization
  effect; the perfect-information ceiling does not convert to resource-honest observable value. Program O
  terminates as a boundary instance, extending the D–K null pattern and the Program K
  cost-efficiency-not-resilience finding.

Either answer yields a terminal outcome. Neither authorizes a learner or opening sealed tapes without its
own preregistered gate. **The sealed tapes must not be opened under the fixed-clock contract until Q14 is
answered fixed-clock** — otherwise it is post-hoc selection of the convention that passes.

## 5. Disposition

`full_des_h_pi_established: True` · `h_obs_confirmed: False` · `learner_authorized: False` ·
`paper2_confirmed: False` · `paper3_authorized: False`. Internal search exhausted; terminal outcome is
this exhaustion certificate plus the exact committed questions Q13/Q14. Custody: diagnostic `e48606e7`,
fit `d67ac97a`, H_PI verdict `f5f2da8d`; sealed tapes intact.
