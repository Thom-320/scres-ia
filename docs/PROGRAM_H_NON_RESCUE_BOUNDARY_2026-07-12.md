# Program H — non-rescue boundary (binding header, frozen 2026-07-12)

```
Program G remains terminal.
Program H estimates whether observable policy value is information-limited
or policy-class-limited.
No risk, signal, metric, physics, horizon, or action parameter is selected
using learner performance.
```

Program H does NOT reopen, retune, or relabel Program G. It answers the ONE scientific ambiguity
Program G left open: Program G showed `J_PI > J_static` (clairvoyant headroom) and, for the specific
policies tested (cover, MPC, depth-3 tree, MaskablePPO), `J_π < J_static`. It did NOT establish
`J*_obs ≤ J_static` — the best POSSIBLE non-anticipative policy might still beat ABAB. Program H
either bounds `J*_obs` below `J_static + δ` (→ information-limited, a formal insufficiency result) or
finds a belief-aware policy that beats ABAB (→ policy-class-limited, and only THEN is a learner
authorized). Every physics/metric/horizon/risk/signal parameter is frozen at the Program G V1.2
values; nothing is chosen by learner performance. Program H is the LAST computational extension on the
stylized spatial contract. The full-DES port is a separate future paper and must not start before the
current manuscript is submitted. All outcomes are pre-committed and publishable.

The primary information contract is O0, exactly matching Program G. Because tempo is semi-Markov,
the Bayes filter must retain dwell age (or an equivalent residual-duration phase); a nine-label tempo
marginal alone is not a sufficient Markov state. O0 may not use realized-demand history that Program G
did not expose. O1 is excluded by default and cannot be fitted or evaluated without a signed domain
record; its absence does not block or alter the O0 audit.
