# Research options after the triple stop

**Decision status:** preserve Option A as the primary publication route; develop
Option B only as a separately preregistered research lane. Do not alter the
terminal claims of Track B, Track C, or Program L v1.

## Option A — The paper already supported by the evidence

The three negative programs form one publishable methodological result:

> **When not to use RL: preregistered gates for DES–RL resilience studies.**

The corpus combines same-contract frontiers, oracle gates, exact-replay
branching, persistent/reset/frozen identification controls, and explicit
non-identifiability boundaries. Together they show that an apparent learned
advantage can disappear when comparator decision rights are aligned and that
training is unwarranted when the tested contract exposes no deployable dynamic
headroom.

The remaining work is editorial consolidation, not additional experiments.
Three overlapping manuscript skeletons — the Track B C&IE pivot, the Track C
extension, and the Program L boundary — must become one paper.

The 18-policy panel and resource frontier also address the thesis's open
questions on inventory–capacity interaction (§8.6.1) and resource-aware
resilience optimization (§8.6.2) using classical policies; RL need not win for
those managerial findings to matter.

## Option B — Program v2: a deliberate change of decision class

If adaptive headroom is pursued again, it must live in a new physical and
decision contract:

1. Emergency reserve downstream of the threatened arc (Op11/theatre), with a
   real replenishment lead time and no replenishment through a failed route.
2. An explicit imperfect 14-day warning channel with 20% false positives and
   20% false negatives, plus a shuffled-alert placebo.
3. Finite, costly emergency capacity so scarcity resembles B0 — the only v1
   setting with action heterogeneity — rather than allowing every actuator to
   remain maximal.
4. A sub-weekly dispatch decision epoch.

Program v2 reuses the Program L environment discipline, exact tapes, branching
gate, fixed normalizers, and retained/reset/frozen harness. It does not reuse
the v1 promotion result. The prior probability of a learned advantage is low:
three decision classes measured headroom ceilings at or below a few percent.
Therefore v2 requires a new preregistration and a terminal pre-training gate.

## Option C — Separate questions using existing evidence

1. A short control/OR paper on commitment lags and hysteresis: smoothed
   persistence outperforming a clairvoyant switcher under lagged implementation.
2. An SMPT-oriented forensic paper on reconstructing a legacy simulation,
   detecting non-identifiable internals, and using a disclosed two-tier fidelity
   contract.

## Option D — Garrido domain validation

Send the one-question attribution email already drafted in
`docs/GARRIDO_ATTRIBUTION_DECISION_2026-07-10.md` and ask where dynamic decisions
matter in the real MFSC. His domain view should inform Program v2 before any
expensive training. Share the boundary corpus as a rigorous, inverted answer to
the 2024 learning agenda.

## Frozen recommendation

Proceed with **A + D** as the publication program. Develop **B** in parallel
only through contract design and pre-training headroom gates. No Program v2 PPO
run is authorized until its new observable branching gate passes. Preserve C
as independent contingencies rather than merging them into the main manuscript.

## Update: preventive v2/v3 execution

The proposed downstream-reserve class was implemented and evaluated without
training PPO. v2 stopped under a fixed 336 h replenishment lead. A separately
preregistered v3 corrected the physical path to Op10 24 h + Op11 availability +
Op12 24 h and used 30 new paired tapes. v3 also stopped:

- the reserve mechanism was live on 30/30 tapes;
- perfect warning improved service 17.86% over zero reserve;
- imperfect warning improved only 1.18% over a shuffled placebo, with CI95
  crossing zero;
- static 15k captured the resource-efficient value.

Therefore Option B is terminal for this reserve-and-warning class. It
strengthens Option A: the physical redesign creates reserve value, but not
sufficient adaptive information value to justify RL.

## Addendum: independent convergence (2026-07-11, PI-requested investigation)

A second, independently-run investigation (same corpus, same verified verdict
commit `4f1a5af`) converged on this exact ranking before the v2/v3 reserve
result was known to it, and now stands strengthened by it:

- **Option A remains the primary route.** The three "failures" are one
  decisive result: *"When not to use RL: preregistered gates for DES–RL
  resilience studies"*, with a triple-measured answer in a validated military
  case — one robust static configuration (I336×S2) is structurally sufficient;
  weekly switching value is exactly zero (100% S1 in branching); engineered
  non-stationarity with priced costs opens ≤2% headroom; every prior apparent
  RL win was a comparator artifact. The downstream-reserve result above is a
  FOURTH independent confirmation, on a fourth decision class.
- **The real next task is not experimentation, it is editorial fusion.**
  Three (now effectively four, counting the reserve gates) overlapping
  manuscript skeletons exist in the repo — the Track B C&IE pivot, the Track C
  extension, and the Program L boundary (plus this reserve addendum) — and
  must become ONE paper before anything else is worth doing here.
- **Bonus already banked:** the 18-policy static panel + cost frontier answers
  the thesis's own open questions (§8.6.1 inventory-capacity synergy, §8.6.2
  cost-aware optimum) with classical methods — managerial content IJPR/C&IE
  want, independent of whether RL ever wins.
- This is judged a genuinely strong, own-account paper opportunity: the
  gate-protocol contribution (methodology for deciding *not* to train, saving
  every PPO seed across four decision classes) is close to unpublished in the
  DES-RL resilience literature.

No new claim, decision, or experiment is authorized by this addendum — it is
a confirmation and a prioritization note. See
`docs/CLAIM_AND_IDENTIFICATION_2026-07-11.md` and
`docs/L_PROGRAM_GATE2_VERDICT_2026-07-10.md` for the governing frozen
contracts this addendum must not contradict.
