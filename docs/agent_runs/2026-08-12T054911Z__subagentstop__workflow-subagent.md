# Agent run — SubagentStop

> **`UNADJUDICATED_DO_NOT_CITE`** — raw agent transcript saved by the `SubagentStop` hook. It may contain WITHDRAWN claims; the canon lives in sealed artifacts and amendments, never here.

- **event**: `SubagentStop`
- **agent_type**: `workflow-subagent`
- **session_id**: `fc8fefc1-10a6-4eea-bcbf-d92791120134`
- **agent_id**: `ae3b35e41b4aa0dd9`
- **cwd**: `<HOME>/Projects/research/scres-ia`
- **transcript_path**: `<HOME>/.claude/projects/-Users-thom-Projects-research-scres-ia/fc8fefc1-10a6-4eea-bcbf-d92791120134.jsonl`
- **saved_at**: `2026-08-12T05:49:11.626392+00:00`
- **commit**: `b68bcf19`
- **answer read from**: `<HOME>/.claude/projects/-Users-thom-Projects-research-scres-ia/fc8fefc1-10a6-4eea-bcbf-d92791120134/subagents/workflows/wf_0c792111-6d8/agent-ae3b35e41b4aa0dd9.jsonl`

## Final response

# Auditoría de reapertura — 8 lanes verificadas

**Nota de método.** Todo número lleva ruta de fichero y ruta de claves JSON. Lo que he derivado yo (restas, cocientes, relojes de pared) va marcado `[derivado]` con la fórmula. Tres artefactos viven sólo en refs de git y no en el árbol de trabajo: se indica el ref. No he modificado nada.

---

## 1. El patrón

**Sí hay patrón, y es la categoría que pediste. De las 8 lanes auditadas más las 3 vecinas que la auditoría destapó, hay cuatro programas que ganaron en media y murieron en una restricción distribucional — y los cinco *near-misses* más pequeños de todo el corpus son todos de ese tipo.**

### 1.1 Los cuatro casos, con el número exacto

| # | programa | ganó en media (campo) | murió en (campo) | barra | distancia |
|---|---|---|---|---|---|
| 1 | **O** (fixed-clock H_obs correctiva) | `inference.estimates["rho75_share90::primary::ret_visible"].estimate` = **+0,0985211781495476**, `simultaneous_lcb95` +0,06595249090039648 · rho90_share75 +0,07347005569899683 (LCB +0,04302851164856679) · rho90_share90 +0,09973538566859201 (LCB +0,05860399810214705). `gates.primary_pass=true`, `placebo_pass=true`, `physical_pass=true` | `ret_visible_cvar10`: rho75_share90 estimate **+0,03501668913903388**, `simultaneous_lcb95` **−0,008577578152472065**; rho90_share75 estimate +0,01953512602297382, LCB **−0,01550692444175086** | no-inferioridad (0) | **−0,008578 / −0,015507** |
| 2 | **Q** (confirmación terminal) | `inference.estimates["rho90_share90::H_OL"].point` = **+0,1172400203711007** [+0,10614476924833394, +0,12833527149386748] contra barra 0,01; y en el mismo endpoint de equidad **vs_open_loop** +0,42674963821814416 (LCB +0,3479271406770903) | `guardrail_inference.estimates["*::worst_product_fill::vs_classical"].lcb95`: **−0,022660984318497703 / −0,02566401458087885 / −0,026322146205590816** (points −0,01035590095948069 / −0,015728386715076814 / −0,004507797808062319) | **−0,02** (`contracts/program_q_frozen_policy_replication_v1.json` → `class_b_integrity_gates.worst_product_fill_margin`) | **−0,002661 / −0,005664 / −0,006322** |
| 3 | **I** (headroom GSA) | `blocks.GP_search_3000001.H_obs` **0,0131** ci95 [0,0102, 0,016]; FRESH_4200001 0,0114 [0,0087, 0,0141]; FRESH_4500001 0,01 [0,0072, 0,0129] — **OOS-estable en tres bloques** | `worst_cssu_fill_delta` **−0,1435 / −0,1282 / −0,1258** | −0,02 (`reason`) | **−0,1235 / −0,1082 / −0,1058** |
| 4 | **G** (triangulación métrica) | `cover_minus_ABAB_ci95.cd_sigmoid` **[+0,00478144092132831, +0,0036769730202516684, +0,005975055890041531]** y `cd_spatial` **[+0,04803884094839869, +0,03725037698014028, +0,05926272124701088]** — LCB>0 en ambas lentes Cobb-Douglas | `cover_minus_ABAB_ci95.worst_cssu_fill` **[−0,173262241113646, −0,2133743825817509, −0,13646179169672787]** | −0,02 (familia) | ≈ −0,116 |

Rutas: (1) `results/program_o/fixed_clock_hobs_corrective_validation_v1/remote_run/artifacts/validation/result.json` [ref `codex/program-o-corrective-execution`]; (2) `results/program_q/confirmation_v1_20260718/artifacts/confirmation/evaluation/result.json` [ref `paper2-program-q-integration`]; (3) `results/headroom_gsa/oos_guardrail_check.json`; (4) `results/program_g/triangulation/verdict.json`.

Un quinto —**H**— cae en la misma familia pero **no** ganó en media: `results/paper2_search/failure_taxonomy.json` → `decision_families` (`H_belief_state_spatial_rescue`) `.h_obs.mean` = 0,0022506304678873725 con ci95 [−0,00021266819632288482, +0,0046005506211061024], que cruza cero. Lo excluyo del recuento.

### 1.2 La taxonomía del propio repo ya lo dice, y subcuenta

`results/paper2_search/failure_taxonomy.json` tiene 17 familias. `fairness_concentration` aparece en **3** de ellas (`G_spatial_commitment`, `H_belief_state_spatial_rescue`, `I_global_scarcity_concurrency_region`) — es la clase de fallo más frecuente después de `observable_conversion` (5) y `physical_magnitude` (4). Pero la taxonomía es anterior a Program Q y a la validación correctiva de O: **con esas dos dentro son 5 de 19 familias**, y las dos añadidas son precisamente las de mayor calidad de evidencia (DES completo, bloques vírgenes, réplica física con 0 fallos).

### 1.3 El ranking de cercanía a la barra — todos distribucionales

`[derivado: LCB − barra]`

1. Q · rho75_share90 · worst_product_fill → **0,002660984318497703**
2. Q · rho90_share75 → 0,005664014580878850
3. Q · rho90_share90 → 0,006322146205590816
4. O · rho75_share90 · ret_visible_cvar10 → 0,008577578152472065
5. O · rho90_share75 → 0,015507

**Los cinco fallos más ajustados del proyecto son la misma cosa: una cota inferior de una restricción distribucional, con el estimador puntual *dentro* del margen en 5 de 5 casos.** Y los cinco vienen de dos programas que comparten el mismo sustrato físico: contención no fungible de dos productos sobre Op5–Op7.

### 1.4 El número que convierte el patrón en una hipótesis accionable

`results/program_o/full_des_hpi_translation_v1/validation_custody_verdict_v1.json`:
- `primary.raw_h_pi` = **0,15275340823389597** (oráculo sin restricciones)
- `primary.safe_h_pi` = **0,15151378920653932** (oráculo obligado a satisfacer el vector completo de guardarraíles por tapa)

`contracts/program_o_full_des_hpi_translation_v1.json` → `safe_oracle_constraints_per_tape.higher_or_equal` = `['visible_rows','ret_full','quantity_ret_full','ret_visible_cvar10','fill_P_C','fill_P_H','worst_product_fill']`.

**Ese vector contiene exactamente los dos endpoints que mataron a O y a Q.** Coste de imponerlos *a nivel clarividente*: `[derivado]` 0,15275340823389597 − 0,15151378920653932 = **0,00123961902735665**, el **0,81 %** del headroom bruto.

En el techo, respetar la equidad y la cola es casi gratis. Las dos políticas que murieron por equidad y por cola **fueron entrenadas y seleccionadas contra la media de ReT**, no contra un objetivo que contuviera la restricción. Eso no es un patrón fabricado: es una asimetría medida entre lo que el oráculo puede hacer y lo que se le pidió al aprendiz.

### 1.5 Lo que el patrón NO dice

Que la restricción sea barata para el oráculo clarividente **no implica** que sea barata para una política observable. El 0,81 % es un límite inferior del coste, no una promesa. Ese es exactamente el falsador de la reapertura número 1.

---

## 2. Ranking de reapertura

**Anclas de coste medidas** (todas reconstruibles; los CPU-h son `[derivado]` a partir de ellas):

- **A1** — H_PI de O, 24 tapas × 65.536 calendarios × DES completo: `outputs/program_o_runs/program-o-full-des-validation-v2-20260715/custody/producer_exit.json` `.started_at_utc`=02:04:41.488614Z → `.finished_at_utc`=02:16:57.099343Z = **735,6 s de reloj**.
- **A2** — ajuste H_obs de O: mismo fichero en `program-o-hobs-fit-v1-20260715`, 02:48:28.388962Z → 02:51:19.912483Z = **171,5 s**.
- **A3** — confirmación completa de Q (256 tapas × 3 celdas × 768 shards, 21.696 replays DES, 10.000 bootstraps), `custody/launch_manifest.json` `.workers`=2: intento fallido 01:14:07.614076Z → 04:05:36.409288Z (10.288,8 s) + intento válido `custody/producer_control.json` `.launched_at` 04:31:57.615364Z → `custody/producer_exit.json` `.finished_at` 05:10:38.505783Z (2.320,9 s) = 12.609,7 s × 2 workers ≈ **7,0 CPU-h**.
- **A4** — maquinaria Fase 3: `results/program_n/phase3_decision_headroom/result.json` `.elapsed_seconds` = **128,0582236669943**; `results/program_n/phase3_decision_surrogate/result.json` `.elapsed_seconds` = **142,78339937501005**.

---

### R1 — Program O: la restricción **dentro** del objetivo

**Por qué vive hoy.** La capacidad nueva es la *pérdida orientada a decisión con optimizador exacto congelado*, y está validada **como instrumento**: `results/program_n/phase3_decision_surrogate/result.json` `.falsifier_summary` → pasan `k1_the_decision_is_live`, `k2_the_optimizer_is_identical`, `k5_a_control_must_be_worse`; fallan sólo `k3` (la pérdida de decisión no bate a MSE: `comparisons.decision_loss_vs_mse.mean` = 5,465747130687742e-06, ci95 [−1,7658198276312633e-06, +1,2697314089006747e-05]) y `k4`. Es decir: **el instrumento funciona, pero se apuntó a una decisión sin premio** — `results/program_n/phase3_decision_headroom/result.json` `.falsifiers.j1_the_headroom_is_material.evidence.computed_from` = `{observed: 6.498828284973168e-05, bar: 0.01}`.

`[derivado]` 0,15151378920653932 / 6,498828284973168e-05 = **2.331×**. La decisión de Program O tiene 2.331 veces el premio de la decisión sobre la que se probó la maquinaria nueva. Y su nulo causal es de una calidad que ninguna otra lane tiene: `validation_custody_verdict_v1.json` `.exact_fungible_null_h_pi` = **0,0** exacto.

**Experimento mínimo.** Sobre el bloque de **desarrollo ya quemado** 7400049–7400072 (`research/seed_custody_registry.json` → `blocks` `reconciled_7400025` [7400025, 7400048] y `reconciled_7430001` [7430001, 7430048], todos `USED_DEVELOPMENT_NOT_VIRGIN`): reajustar el belief-MPC de reloj fijo con dos objetivos — el actual (media de `ret_visible`) y uno penalizado por `ret_visible_cvar10` — y medir el par (Δmedia, ΔCVaR10) en las tres celdas. Grado desarrollo, sin pretensión confirmatoria.

**Falsador que PUEDE fallar (dos, y ambos pueden):**
- **F1 — «la penalización tiene autoridad»**: el arm penalizado debe mover `ret_visible_cvar10` en el sentido correcto con IC que excluye cero frente al arm no penalizado, sobre las mismas tapas. *Puede fallar* porque el 0,81 % es un coste medido sólo en el oráculo clarividente; si la cola de la política observable está determinada por la física de la contención y no por la regla, la penalización no la mueve y la lane muere ahí, en una tarde.
- **F2 — «sobrevive una ventaja media material»**: tras F1, la media debe conservar LCB95 > 0. *Puede fallar* porque las ventajas de +0,0985/+0,0735/+0,0997 podrían estar compradas íntegramente en la cola inferior; si es así, F1 pasa y F2 falla, y eso cierra la familia O/Q **por física**, no por instrumento — que es el resultado que hoy no tenemos.

**Semillas.** Desarrollo sobre tapas consumidas. **Ninguna semilla nueva.** Una decisión confirmatoria sí las exige: `research/seed_custody_registry.json` → `rules.program_q_and_program_o_are_immutable` = **True**, `new_seed_opening` = **False**, `scientific_execution_authorized` = **False**, `status` = `BASELINE_INVENTORY_INCOMPLETE_NO_NEW_SEEDS_AUTHORIZED`; y `results/program_o/fixed_clock_hobs_corrective_validation_v1/independent_audit_v1.json` `.claim_boundary.second_rescue_forbidden` = **true**. **Esto no es un rescate de O: es un programa nuevo que hereda su física.**

**Coste.** `[derivado de A2 y A3]` ajuste: 171,5 s × 2 objetivos × 8 semillas ≈ 0,76 h de reloj. Evaluación: 144 tapas-celda frente a las 768 de A3 → 7,0 × (144/768) ≈ 1,3 CPU-h. **Total ≈ 4–8 CPU-h.** Local, una noche.

---

### R2 — Program O: el nulo de Jensen sobre el techo de 0,1515

**Por qué vive hoy.** El nulo de permutación de Jensen no existía cuando O cerró. `primary.safe_h_pi` es **media-de-máximos-por-tapa menos máximo-de-medias** — exactamente la forma a la que el nulo aplica. Y aquí está el matiz que nadie ha señalado: **el nulo fungible NO controla el sesgo de Jensen**. `exact_fungible_null_h_pi` = 0,0 *exacto* sólo puede salir si bajo fungibilidad los 65.536 calendarios son idénticos, es decir, si la varianza entre acciones se anula — en ese caso no hay sesgo que acumular. Es un nulo de **física**, no de **estimador**. El estimador nunca ha sido testeado.

**Experimento mínimo.** Re-análisis puro sobre `outputs/program_o_runs/program-o-full-des-validation-v2-20260715/artifacts/validation/raw_calendar_matrix/rho75_share90__centered_minority_v1/tape_74001*.npz` (74 MB en disco, 4 perfiles presentes). Permutar la etiqueta de calendario dentro de cada tapa y recomputar `safe_h_pi` 1.000 veces.

**Falsador.** «Observado > p95 del nulo». *Puede fallar*: el techo extendido ya murió a este nulo en otra rama del proyecto (`results/program_n/phase3_decision_headroom/result.json` `.falsifiers.j2_it_survives_its_own_jensen_null.evidence.computed_from` = `{observed: 6.498828284973168e-05, null_mean: 0.003977886172123899, null_p95: 0.004467317109579483, p_value: 1.0}` — el nulo fue **61 veces mayor** que el observado). No hay razón a priori para que 0,1515 sobreviva y 6,5e-05 no; hay que medirlo.

**Semillas.** Cero. Matrices en disco.

**Coste.** `[derivado]` < **0,5 CPU-h**. Minutos.

**Ordena esto ANTES que R1.** Si el 0,1515 no sobrevive a Jensen, R1 no tiene premio y toda la familia O/Q se cierra en una hora en vez de en una semana.

---

### R3 — Program I: la equidad como restricción de la búsqueda, no como filtro posterior

**Por qué vive hoy.** I es el caso más limpio de «gané en media, OOS-estable en tres bloques, me mató la equidad»: H_obs 0,0131/0,0114/0,0100 con ci95 estrictamente positivos, `worst_cssu_fill_delta` −0,1435/−0,1282/−0,1258. Lo que cambia: la **medición de headroom de decisión ANTES de entrenar** permite preguntar lo que nunca se preguntó — *¿cuánto headroom queda si la equidad entra en la definición del óptimo?* El GP buscó θ maximizando ReT agregado; nunca se buscó sobre ReT sujeto a `worst_cssu_fill_delta ≥ −0,02`.

**Experimento mínimo.** Recorrer la rejilla ya evaluada (`results/headroom_gsa/all_cells_reconstruction.json`) y recomputar el óptimo restringido. Si el máximo de H_obs sujeto a la restricción es < 0,01, la lane está muerta de forma **demostrable** y no por accidente de selección.

**Falsador.** «Existe θ con H_obs LCB95 > 0,01 y `worst_cssu_fill_delta` > −0,02». *Puede fallar*, y probablemente falla: la brecha actual es de 5,3 a 7,2× el umbral `[derivado: 0,1435/0,02 = 7,18]`, no es un near-miss. Pero el resultado convierte «falló el guardarraíl» en «no existe punto factible», que es una frase muy distinta en un paper.

**Semillas.** Cero, si la reconstrucción de celdas cubre la rejilla. Si no, tapas ya consumidas (3000001, 4200001, 4500001).

**Coste.** `[derivado]` re-análisis: **< 1 CPU-h**. Si hay que re-evaluar celdas: ~5–15 CPU-h.

---

### R4 — Program G: ¿el +0,048 de `cd_spatial` es señal o es Jensen?

**Por qué vive hoy.** El nulo de Jensen y la clase comparadora ampliada. G tiene dos lentes Cobb-Douglas en las que `cover` bate a ABAB con LCB > 0 (`cd_sigmoid` +0,00478, `cd_spatial` +0,04804) y siete métricas en las que gana ABAB o pierde cover. Hoy sabemos dos cosas que no sabíamos: que una reescalada monótona suave mueve H de 0,0195 a 0,0742 sin tocar el orden, y que las medias-de-máximos son positivas bajo ruido puro.

**Experimento mínimo.** Aplicar el nulo de permutación a `cover_minus_ABAB` en las dos lentes CD, y añadir a la clase comparadora las variantes con lag (GBDT/RF/GP) que hoy existen y entonces no.

**Falsador.** «`cd_spatial` sobrevive al nulo Y a la clase ampliada». *Puede fallar por los dos lados*: el propio artefacto se declara `kind: "exploratory_metric_sensitivity"` y `phi_kappa_held_constant_disclosed: true` — con φ y κ congelados el índice está movido sólo por ζ/ε/τ, lo que es justamente el régimen donde una reescalada puede fabricar la ventaja.

**Semillas.** Cero. `test_seed_start` 1030001 ya consumidas; es re-análisis.

**Coste.** `[derivado]` **1–3 CPU-h**.

**Advertencia.** Aunque sobreviva, G sigue muerta en `worst_cssu_fill` −0,1733 y en `ret_order` −0,0209. Esto reabre una **pregunta metodológica**, no una lane.

---

### R5 — Program Q: la equidad en la pérdida (programa nuevo, no rescate)

**Por qué vive hoy.** Los tres *near-misses* más pequeños del corpus. `integrity_gates` de Q: seis de siete en `true`, sólo `worst_product_fill_noninferior` en `false`. Por las reglas del propio contrato, la rama científica que sobrevivió es adaptación replicada + equivalencia práctica; **lo único que separa a Q de `PASS_Q_LEARNED_ADAPTATION_CLASSICALLY_EQUIVALENT` es un guardarraíl de equidad cuyo estimador puntual está dentro del margen en las tres celdas.**

**Por qué va el último y no el primero.** Tres razones duras, todas verificadas:
1. `research/seed_custody_registry.json` `rules.program_q_and_program_o_are_immutable` = True. No hay re-adjudicación posible.
2. Q **no ganó en media contra el comparador que lo mató**: `inference.estimates["*::Delta_N"].point` = −0,0015852439830280618 / −0,0007245803665938411 / −0,00041037188947301573 frente a `max(classical)`. Ganó sólo contra el lazo abierto (+0,0795/+0,0725/+0,1172). Filarlo como «ganó en media y murió en la cola» sin ese matiz sobrevende la lane.
3. La multiplicidad **no** es la culpable a nivel de lane: `[derivado de point y se, con z=1,645 en lugar de `guardrail_inference.simultaneous_critical`=2,1573137704814935]` sólo rho75_share90 pasaría (−0,019739); rho90_share75 (−0,023304) y rho90_share90 (−0,021142) siguen fallando. Quitar la corrección entera no salva la puerta.

**Experimento mínimo.** Reentrenar con `worst_product_fill` en el objetivo (Lagrangiano o CVaR sobre el producto peor servido) y confirmar sobre bloque virgen nuevo.

**Falsador.** «La política con equidad en la pérdida conserva LCB95(H_OL) ≥ 0,01 y `worst_product_fill` LCB95 ≥ −0,02 en las tres celdas». *Puede fallar* en cualquiera de los dos brazos, y el fallo interesante es el primero: si al meter la equidad se pierde la adaptación, la conclusión es que la adaptación **era** la concentración.

**Semillas.** **Bloque virgen nuevo obligatorio** + excepción PI documentada (patrón de `pi_exceptions` en el registro). El bloque 7490001–7490256 está `BURNED`.

**Coste.** Evaluación `[derivado de A3]` ≈ **7 CPU-h medidos**. **El entrenamiento no está priceado en ningún artefacto que haya podido localizar** — no doy una cifra que no puedo reconstruir. Ordena R1 primero precisamente porque R1 responde la misma pregunta física por ~5 CPU-h y sin abrir semillas.

---

## 3. Lo que NO reabriría

**K / K2 / K3 — muerta por física, y con el certificado puesto.** `results/k2/strong_comparators.json` bajo la física corregida (`cell.shelf_life`=156): `decomp.static_minus_mpc` = [201,9, **−1535,6**, +1964,4] — cruza cero; `decomp.static_minus_base_stock` = [−5860,6, −7400,8, −4263,5] — el base-stock es *peor* que una constante; `decomp.best_observable_minus_clairvoyant_EVPI` = [15820,2, 14754,4, 16896,2], la brecha entera es EVPI no convertible. Y `results/k3/development_screen.json` `.restricted_pi_ret_delta` = [0,0034660218253968265, 0,0006423611111111116, 0,006783699156746036] — **el techo clarividente restringido es 0,0035 contra una barra de 0,01**. Un comparador mejor no crea headroom donde el oráculo mismo se queda a un tercio de la barra. La clase ampliada la mataría más rápido, no la salvaría.

**Track C — muerta por magnitud, 7,6× por debajo.** `outputs/experiments/track_c_gates_iter3_2026-07-10/c1_verdict.json` `.switcher_minus_constant_J.mean` = 2,2594235099883376e-05 con ci95 [1,3957053322321964e-05, 3,267935557448121e-05] — **limpio, 23/24 tapas positivas** — contra `.threshold_005_ret_base` = 1,7168230567775187e-04. `[derivado]` incluso el **extremo superior** del IC está 1,39e-04 por debajo de la barra. Un IC que excluye cero y está dos órdenes de magnitud bajo la materialidad no es un near-miss: es la definición de ausencia de headroom. Y el supuesto defecto de instrumento del flag C2 está refutado (el conjunto `c1.passed` se añadió en 185d52ca, 3m49s *después* de que el artefacto se escribiera, y se ve disparando en iter5).

**Track B no-neuronal — muerta por comparador, y ya se re-corrió.** `results/track_b_nonneural/result.json` `.network_minus_constant.trained_mlp` = 2,176012221662134 sin ningún IC para ningún brazo de red; `results/program_n/gate_a2_track_b/result.json` `.quality_premium_over_best_nonneural` = {mean −0,5593679111583943, lcb95 −0,7476011488858794, ucb95 −0,3856069246810923, favourable 7/48} contra sesoi 0,01. `[derivado]` distancia −0,7576. **La clase ampliada ya se aplicó y el resultado empeoró en 2,7 unidades.** Volver a ampliarla no puede invertir el signo.

**Program G como *lane de valor*.** R4 reabre una pregunta sobre métricas. La lane de valor está cerrada: `results/program_g/terminal_metric_audit/verdict.json` `.policy_audits.cover.paired_delta_ci95.ret_order` = [−0,02317453625750402, −0,028162880439771405, −0,018928269844804356] y `.unfulfilled_rations_at_horizon` = [+3370,2775, +2831,2709375, +3970,8655624999997]. Pierde en la métrica canónica **y** en masa de raciones no servidas. No hay comparador que arregle eso.

**Program H.** `h_obs.mean` 0,0022506304678873725 con ci95 que cruza cero, favorable en 19 % de las tapas, `worst-CSSU delta −0,0385`. Ni ganó en media ni está cerca del guardarraíl. No entra.

**Y una regla general, sin excepciones:** una lane que murió con `physics_no_headroom` — Track C, K2, la decisión de configuración con sus 6,5e-05 — **no revive por tener comparadores mejores ni pérdidas mejores**. Ambas capacidades nuevas actúan sobre *quién decide mejor*, no sobre *cuánto vale decidir*. Cuando `H_PI` está bajo la barra, el mejor decisor posible sigue estando bajo la barra. Es exactamente la jerarquía del CLAUDE.md: crear headroom primero.

---

## 4. La apuesta única

**R1: Program O, la restricción de cola dentro del objetivo — precedida por R2 como filtro de 30 minutos.**

Cuatro razones, todas con número:

1. **Es el único sitio del proyecto con premio medido.** `safe_h_pi` = 0,15151378920653932, `simultaneous_lcb95` = 0,1156159088651399 contra barra 0,01 (`contracts/program_o_full_des_hpi_translation_v1.json` → `validation_pass_rule.simultaneous_one_sided_lcb95_safe_h_pi_minimum`), con el nulo causal `exact_fungible_null_h_pi` = **0,0 exacto** y réplica en bloque disyunto (`development_custody_verdict_v1.json` `.primary.safe_h_pi` = 0,15058413468153348). `[derivado]` **2.331×** el premio de la decisión sobre la que se validó la maquinaria de Fase 3.

2. **Es el único caso de victoria en media *contra el comparador fuerte*** con todas las puertas de media pasando (`gates.primary_pass`, `action_pass`, `placebo_pass`, `physical_pass` = true; 27/27 placebos batidos) y muriendo sólo en la cola. Q empató con el clásico; I y G ganaron sólo en métricas laterales. O ganó de verdad.

3. **La hipótesis es específica y está medida, no inventada.** El vector de restricciones que mató a O contiene `ret_visible_cvar10` y `worst_product_fill`, y el oráculo lo satisface pagando `[derivado]` 0,00123961902735665 = **0,81 %** del headroom. La política nunca tuvo esa restricción en su objetivo. El experimento pregunta exactamente eso.

4. **Cuesta menos de una noche y decide en cualquier dirección.** `[derivado de A2/A3]` ~4–8 CPU-h, cero semillas nuevas, tapas ya quemadas. Si F1 falla, la cola no es controlable y la familia O/Q se cierra **por física** — que es una frase mucho mejor para el paper que «falló un guardarraíl». Si F1 pasa y F2 falla, sabemos que la ventaja media *era* la cola. Si ambos pasan, hay por primera vez en el proyecto un candidato a conversión observable segura, y **entonces** se justifica pedir al PI la excepción de semillas vírgenes que hoy el registro prohíbe.

**Condición de ejecución.** No es un rescate de Program O y no puede presentarse como tal: `independent_audit_v1.json` `.claim_boundary` marca `program_o_closed` = true y `second_rescue_forbidden` = true, y el registro marca O y Q como inmutables. Es un **programa nuevo con contrato nuevo** que hereda la física de O, se ejecuta en grado desarrollo sobre tapas quemadas, y sólo pide semillas si sus dos falsadores pasan.

**Y córrelo después de R2.** Media hora de nulo de Jensen sobre `raw_calendar_matrix` decide si el 0,15151 es un premio o un artefacto del estimador. Es la prueba más barata y más letal que queda en el repositorio, y nunca se ha hecho.

## Raw payload

```json
{
 "agent_id": "ae3b35e41b4aa0dd9",
 "agent_transcript_path": "<HOME>/.claude/projects/-Users-thom-Projects-research-scres-ia/fc8fefc1-10a6-4eea-bcbf-d92791120134/subagents/workflows/wf_0c792111-6d8/agent-ae3b35e41b4aa0dd9.jsonl",
 "agent_type": "workflow-subagent",
 "background_tasks": [
  {
   "description": "Find the lanes that came closest to a claim, verify their numbers against artifacts, and rank what is reopenable",
   "id": "wydnbmt6b",
   "name": "closest-losses-audit",
   "status": "running",
   "type": "workflow"
  }
 ],
 "cwd": "<HOME>/Projects/research/scres-ia",
 "effort": {
  "level": "high"
 },
 "hook_event_name": "SubagentStop",
 "last_assistant_message": "# Auditoría de reapertura — 8 lanes verificadas\n\n**Nota de método.** Todo número lleva ruta de fichero y ruta de claves JSON. Lo que he derivado yo (restas, cocientes, relojes de pared) va marcado `[derivado]` con la fórmula. Tres artefactos viven sólo en refs de git y no en el árbol de trabajo: se indica el ref. No he modificado nada.\n\n---\n\n## 1. El patrón\n\n**Sí hay patrón, y es la categoría que pediste. De las 8 lanes auditadas más las 3 vecinas que la auditoría destapó, hay cuatro programas que ganaron en media y murieron en una restricción distribucional — y los cinco *near-misses* más pequeños de todo el corpus son todos de ese tipo.**\n\n### 1.1 Los cuatro casos, con el número exacto\n\n| # | programa | ganó en media (campo) | murió en (campo) | barra | distancia |\n|---|---|---|---|---|---|\n| 1 | **O** (fixed-clock H_obs correctiva) | `inference.estimates[\"rho75_share90::primary::ret_visible\"].estimate` = **+0,0985211781495476**, `simultaneous_lcb95` +0,06595249090039648 · rho90_share75 +0,07347005569899683 (LCB +0,04302851164856679) · rho90_share90 +0,09973538566859201 (LCB +0,05860399810214705). `gates.primary_pass=true`, `placebo_pass=true`, `physical_pass=true` | `ret_visible_cvar10`: rho75_share90 estimate **+0,03501668913903388**, `simultaneous_lcb95` **−0,008577578152472065**; rho90_share75 estimate +0,01953512602297382, LCB **−0,01550692444175086** | no-inferioridad (0) | **−0,008578 / −0,015507** |\n| 2 | **Q** (confirmación terminal) | `inference.estimates[\"rho90_share90::H_OL\"].point` = **+0,1172400203711007** [+0,10614476924833394, +0,12833527149386748] contra barra 0,01; y en el mismo endpoint de equidad **vs_open_loop** +0,42674963821814416 (LCB +0,3479271406770903) | `guardrail_inference.estimates[\"*::worst_product_fill::vs_classical\"].lcb95`: **−0,022660984318497703 / −0,02566401458087885 / −0,026322146205590816** (points −0,01035590095948069 / −0,015728386715076814 / −0,004507797808062319) | **−0,02** (`contracts/program_q_frozen_policy_replication_v1.json` → `class_b_integrity_gates.worst_product_fill_margin`) | **−0,002661 / −0,005664 / −0,006322** |\n| 3 | **I** (headroom GSA) | `blocks.GP_search_3000001.H_obs` **0,0131** ci95 [0,0102, 0,016]; FRESH_4200001 0,0114 [0,0087, 0,0141]; FRESH_4500001 0,01 [0,0072, 0,0129] — **OOS-estable en tres bloques** | `worst_cssu_fill_delta` **−0,1435 / −0,1282 / −0,1258** | −0,02 (`reason`) | **−0,1235 / −0,1082 / −0,1058** |\n| 4 | **G** (triangulación métrica) | `cover_minus_ABAB_ci95.cd_sigmoid` **[+0,00478144092132831, +0,0036769730202516684, +0,005975055890041531]** y `cd_spatial` **[+0,04803884094839869, +0,03725037698014028, +0,05926272124701088]** — LCB>0 en ambas lentes Cobb-Douglas | `cover_minus_ABAB_ci95.worst_cssu_fill` **[−0,173262241113646, −0,2133743825817509, −0,13646179169672787]** | −0,02 (familia) | ≈ −0,116 |\n\nRutas: (1) `results/program_o/fixed_clock_hobs_corrective_validation_v1/remote_run/artifacts/validation/result.json` [ref `codex/program-o-corrective-execution`]; (2) `results/program_q/confirmation_v1_20260718/artifacts/confirmation/evaluation/result.json` [ref `paper2-program-q-integration`]; (3) `results/headroom_gsa/oos_guardrail_check.json`; (4) `results/program_g/triangulation/verdict.json`.\n\nUn quinto —**H**— cae en la misma familia pero **no** ganó en media: `results/paper2_search/failure_taxonomy.json` → `decision_families` (`H_belief_state_spatial_rescue`) `.h_obs.mean` = 0,0022506304678873725 con ci95 [−0,00021266819632288482, +0,0046005506211061024], que cruza cero. Lo excluyo del recuento.\n\n### 1.2 La taxonomía del propio repo ya lo dice, y subcuenta\n\n`results/paper2_search/failure_taxonomy.json` tiene 17 familias. `fairness_concentration` aparece en **3** de ellas (`G_spatial_commitment`, `H_belief_state_spatial_rescue`, `I_global_scarcity_concurrency_region`) — es la clase de fallo más frecuente después de `observable_conversion` (5) y `physical_magnitude` (4). Pero la taxonomía es anterior a Program Q y a la validación correctiva de O: **con esas dos dentro son 5 de 19 familias**, y las dos añadidas son precisamente las de mayor calidad de evidencia (DES completo, bloques vírgenes, réplica física con 0 fallos).\n\n### 1.3 El ranking de cercanía a la barra — todos distribucionales\n\n`[derivado: LCB − barra]`\n\n1. Q · rho75_share90 · worst_product_fill → **0,002660984318497703**\n2. Q · rho90_share75 → 0,005664014580878850\n3. Q · rho90_share90 → 0,006322146205590816\n4. O · rho75_share90 · ret_visible_cvar10 → 0,008577578152472065\n5. O · rho90_share75 → 0,015507\n\n**Los cinco fallos más ajustados del proyecto son la misma cosa: una cota inferior de una restricción distribucional, con el estimador puntual *dentro* del margen en 5 de 5 casos.** Y los cinco vienen de dos programas que comparten el mismo sustrato físico: contención no fungible de dos productos sobre Op5–Op7.\n\n### 1.4 El número que convierte el patrón en una hipótesis accionable\n\n`results/program_o/full_des_hpi_translation_v1/validation_custody_verdict_v1.json`:\n- `primary.raw_h_pi` = **0,15275340823389597** (oráculo sin restricciones)\n- `primary.safe_h_pi` = **0,15151378920653932** (oráculo obligado a satisfacer el vector completo de guardarraíles por tapa)\n\n`contracts/program_o_full_des_hpi_translation_v1.json` → `safe_oracle_constraints_per_tape.higher_or_equal` = `['visible_rows','ret_full','quantity_ret_full','ret_visible_cvar10','fill_P_C','fill_P_H','worst_product_fill']`.\n\n**Ese vector contiene exactamente los dos endpoints que mataron a O y a Q.** Coste de imponerlos *a nivel clarividente*: `[derivado]` 0,15275340823389597 − 0,15151378920653932 = **0,00123961902735665**, el **0,81 %** del headroom bruto.\n\nEn el techo, respetar la equidad y la cola es casi gratis. Las dos políticas que murieron por equidad y por cola **fueron entrenadas y seleccionadas contra la media de ReT**, no contra un objetivo que contuviera la restricción. Eso no es un patrón fabricado: es una asimetría medida entre lo que el oráculo puede hacer y lo que se le pidió al aprendiz.\n\n### 1.5 Lo que el patrón NO dice\n\nQue la restricción sea barata para el oráculo clarividente **no implica** que sea barata para una política observable. El 0,81 % es un límite inferior del coste, no una promesa. Ese es exactamente el falsador de la reapertura número 1.\n\n---\n\n## 2. Ranking de reapertura\n\n**Anclas de coste medidas** (todas reconstruibles; los CPU-h son `[derivado]` a partir de ellas):\n\n- **A1** — H_PI de O, 24 tapas × 65.536 calendarios × DES completo: `outputs/program_o_runs/program-o-full-des-validation-v2-20260715/custody/producer_exit.json` `.started_at_utc`=02:04:41.488614Z → `.finished_at_utc`=02:16:57.099343Z = **735,6 s de reloj**.\n- **A2** — ajuste H_obs de O: mismo fichero en `program-o-hobs-fit-v1-20260715`, 02:48:28.388962Z → 02:51:19.912483Z = **171,5 s**.\n- **A3** — confirmación completa de Q (256 tapas × 3 celdas × 768 shards, 21.696 replays DES, 10.000 bootstraps), `custody/launch_manifest.json` `.workers`=2: intento fallido 01:14:07.614076Z → 04:05:36.409288Z (10.288,8 s) + intento válido `custody/producer_control.json` `.launched_at` 04:31:57.615364Z → `custody/producer_exit.json` `.finished_at` 05:10:38.505783Z (2.320,9 s) = 12.609,7 s × 2 workers ≈ **7,0 CPU-h**.\n- **A4** — maquinaria Fase 3: `results/program_n/phase3_decision_headroom/result.json` `.elapsed_seconds` = **128,0582236669943**; `results/program_n/phase3_decision_surrogate/result.json` `.elapsed_seconds` = **142,78339937501005**.\n\n---\n\n### R1 — Program O: la restricción **dentro** del objetivo\n\n**Por qué vive hoy.** La capacidad nueva es la *pérdida orientada a decisión con optimizador exacto congelado*, y está validada **como instrumento**: `results/program_n/phase3_decision_surrogate/result.json` `.falsifier_summary` → pasan `k1_the_decision_is_live`, `k2_the_optimizer_is_identical`, `k5_a_control_must_be_worse`; fallan sólo `k3` (la pérdida de decisión no bate a MSE: `comparisons.decision_loss_vs_mse.mean` = 5,465747130687742e-06, ci95 [−1,7658198276312633e-06, +1,2697314089006747e-05]) y `k4`. Es decir: **el instrumento funciona, pero se apuntó a una decisión sin premio** — `results/program_n/phase3_decision_headroom/result.json` `.falsifiers.j1_the_headroom_is_material.evidence.computed_from` = `{observed: 6.498828284973168e-05, bar: 0.01}`.\n\n`[derivado]` 0,15151378920653932 / 6,498828284973168e-05 = **2.331×**. La decisión de Program O tiene 2.331 veces el premio de la decisión sobre la que se probó la maquinaria nueva. Y su nulo causal es de una calidad que ninguna otra lane tiene: `validation_custody_verdict_v1.json` `.exact_fungible_null_h_pi` = **0,0** exacto.\n\n**Experimento mínimo.** Sobre el bloque de **desarrollo ya quemado** 7400049–7400072 (`research/seed_custody_registry.json` → `blocks` `reconciled_7400025` [7400025, 7400048] y `reconciled_7430001` [7430001, 7430048], todos `USED_DEVELOPMENT_NOT_VIRGIN`): reajustar el belief-MPC de reloj fijo con dos objetivos — el actual (media de `ret_visible`) y uno penalizado por `ret_visible_cvar10` — y medir el par (Δmedia, ΔCVaR10) en las tres celdas. Grado desarrollo, sin pretensión confirmatoria.\n\n**Falsador que PUEDE fallar (dos, y ambos pueden):**\n- **F1 — «la penalización tiene autoridad»**: el arm penalizado debe mover `ret_visible_cvar10` en el sentido correcto con IC que excluye cero frente al arm no penalizado, sobre las mismas tapas. *Puede fallar* porque el 0,81 % es un coste medido sólo en el oráculo clarividente; si la cola de la política observable está determinada por la física de la contención y no por la regla, la penalización no la mueve y la lane muere ahí, en una tarde.\n- **F2 — «sobrevive una ventaja media material»**: tras F1, la media debe conservar LCB95 > 0. *Puede fallar* porque las ventajas de +0,0985/+0,0735/+0,0997 podrían estar compradas íntegramente en la cola inferior; si es así, F1 pasa y F2 falla, y eso cierra la familia O/Q **por física**, no por instrumento — que es el resultado que hoy no tenemos.\n\n**Semillas.** Desarrollo sobre tapas consumidas. **Ninguna semilla nueva.** Una decisión confirmatoria sí las exige: `research/seed_custody_registry.json` → `rules.program_q_and_program_o_are_immutable` = **True**, `new_seed_opening` = **False**, `scientific_execution_authorized` = **False**, `status` = `BASELINE_INVENTORY_INCOMPLETE_NO_NEW_SEEDS_AUTHORIZED`; y `results/program_o/fixed_clock_hobs_corrective_validation_v1/independent_audit_v1.json` `.claim_boundary.second_rescue_forbidden` = **true**. **Esto no es un rescate de O: es un programa nuevo que hereda su física.**\n\n**Coste.** `[derivado de A2 y A3]` ajuste: 171,5 s × 2 objetivos × 8 semillas ≈ 0,76 h de reloj. Evaluación: 144 tapas-celda frente a las 768 de A3 → 7,0 × (144/768) ≈ 1,3 CPU-h. **Total ≈ 4–8 CPU-h.** Local, una noche.\n\n---\n\n### R2 — Program O: el nulo de Jensen sobre el techo de 0,1515\n\n**Por qué vive hoy.** El nulo de permutación de Jensen no existía cuando O cerró. `primary.safe_h_pi` es **media-de-máximos-por-tapa menos máximo-de-medias** — exactamente la forma a la que el nulo aplica. Y aquí está el matiz que nadie ha señalado: **el nulo fungible NO controla el sesgo de Jensen**. `exact_fungible_null_h_pi` = 0,0 *exacto* sólo puede salir si bajo fungibilidad los 65.536 calendarios son idénticos, es decir, si la varianza entre acciones se anula — en ese caso no hay sesgo que acumular. Es un nulo de **física**, no de **estimador**. El estimador nunca ha sido testeado.\n\n**Experimento mínimo.** Re-análisis puro sobre `outputs/program_o_runs/program-o-full-des-validation-v2-20260715/artifacts/validation/raw_calendar_matrix/rho75_share90__centered_minority_v1/tape_74001*.npz` (74 MB en disco, 4 perfiles presentes). Permutar la etiqueta de calendario dentro de cada tapa y recomputar `safe_h_pi` 1.000 veces.\n\n**Falsador.** «Observado > p95 del nulo». *Puede fallar*: el techo extendido ya murió a este nulo en otra rama del proyecto (`results/program_n/phase3_decision_headroom/result.json` `.falsifiers.j2_it_survives_its_own_jensen_null.evidence.computed_from` = `{observed: 6.498828284973168e-05, null_mean: 0.003977886172123899, null_p95: 0.004467317109579483, p_value: 1.0}` — el nulo fue **61 veces mayor** que el observado). No hay razón a priori para que 0,1515 sobreviva y 6,5e-05 no; hay que medirlo.\n\n**Semillas.** Cero. Matrices en disco.\n\n**Coste.** `[derivado]` < **0,5 CPU-h**. Minutos.\n\n**Ordena esto ANTES que R1.** Si el 0,1515 no sobrevive a Jensen, R1 no tiene premio y toda la familia O/Q se cierra en una hora en vez de en una semana.\n\n---\n\n### R3 — Program I: la equidad como restricción de la búsqueda, no como filtro posterior\n\n**Por qué vive hoy.** I es el caso más limpio de «gané en media, OOS-estable en tres bloques, me mató la equidad»: H_obs 0,0131/0,0114/0,0100 con ci95 estrictamente positivos, `worst_cssu_fill_delta` −0,1435/−0,1282/−0,1258. Lo que cambia: la **medición de headroom de decisión ANTES de entrenar** permite preguntar lo que nunca se preguntó — *¿cuánto headroom queda si la equidad entra en la definición del óptimo?* El GP buscó θ maximizando ReT agregado; nunca se buscó sobre ReT sujeto a `worst_cssu_fill_delta ≥ −0,02`.\n\n**Experimento mínimo.** Recorrer la rejilla ya evaluada (`results/headroom_gsa/all_cells_reconstruction.json`) y recomputar el óptimo restringido. Si el máximo de H_obs sujeto a la restricción es < 0,01, la lane está muerta de forma **demostrable** y no por accidente de selección.\n\n**Falsador.** «Existe θ con H_obs LCB95 > 0,01 y `worst_cssu_fill_delta` > −0,02». *Puede fallar*, y probablemente falla: la brecha actual es de 5,3 a 7,2× el umbral `[derivado: 0,1435/0,02 = 7,18]`, no es un near-miss. Pero el resultado convierte «falló el guardarraíl» en «no existe punto factible», que es una frase muy distinta en un paper.\n\n**Semillas.** Cero, si la reconstrucción de celdas cubre la rejilla. Si no, tapas ya consumidas (3000001, 4200001, 4500001).\n\n**Coste.** `[derivado]` re-análisis: **< 1 CPU-h**. Si hay que re-evaluar celdas: ~5–15 CPU-h.\n\n---\n\n### R4 — Program G: ¿el +0,048 de `cd_spatial` es señal o es Jensen?\n\n**Por qué vive hoy.** El nulo de Jensen y la clase comparadora ampliada. G tiene dos lentes Cobb-Douglas en las que `cover` bate a ABAB con LCB > 0 (`cd_sigmoid` +0,00478, `cd_spatial` +0,04804) y siete métricas en las que gana ABAB o pierde cover. Hoy sabemos dos cosas que no sabíamos: que una reescalada monótona suave mueve H de 0,0195 a 0,0742 sin tocar el orden, y que las medias-de-máximos son positivas bajo ruido puro.\n\n**Experimento mínimo.** Aplicar el nulo de permutación a `cover_minus_ABAB` en las dos lentes CD, y añadir a la clase comparadora las variantes con lag (GBDT/RF/GP) que hoy existen y entonces no.\n\n**Falsador.** «`cd_spatial` sobrevive al nulo Y a la clase ampliada». *Puede fallar por los dos lados*: el propio artefacto se declara `kind: \"exploratory_metric_sensitivity\"` y `phi_kappa_held_constant_disclosed: true` — con φ y κ congelados el índice está movido sólo por ζ/ε/τ, lo que es justamente el régimen donde una reescalada puede fabricar la ventaja.\n\n**Semillas.** Cero. `test_seed_start` 1030001 ya consumidas; es re-análisis.\n\n**Coste.** `[derivado]` **1–3 CPU-h**.\n\n**Advertencia.** Aunque sobreviva, G sigue muerta en `worst_cssu_fill` −0,1733 y en `ret_order` −0,0209. Esto reabre una **pregunta metodológica**, no una lane.\n\n---\n\n### R5 — Program Q: la equidad en la pérdida (programa nuevo, no rescate)\n\n**Por qué vive hoy.** Los tres *near-misses* más pequeños del corpus. `integrity_gates` de Q: seis de siete en `true`, sólo `worst_product_fill_noninferior` en `false`. Por las reglas del propio contrato, la rama científica que sobrevivió es adaptación replicada + equivalencia práctica; **lo único que separa a Q de `PASS_Q_LEARNED_ADAPTATION_CLASSICALLY_EQUIVALENT` es un guardarraíl de equidad cuyo estimador puntual está dentro del margen en las tres celdas.**\n\n**Por qué va el último y no el primero.** Tres razones duras, todas verificadas:\n1. `research/seed_custody_registry.json` `rules.program_q_and_program_o_are_immutable` = True. No hay re-adjudicación posible.\n2. Q **no ganó en media contra el comparador que lo mató**: `inference.estimates[\"*::Delta_N\"].point` = −0,0015852439830280618 / −0,0007245803665938411 / −0,00041037188947301573 frente a `max(classical)`. Ganó sólo contra el lazo abierto (+0,0795/+0,0725/+0,1172). Filarlo como «ganó en media y murió en la cola» sin ese matiz sobrevende la lane.\n3. La multiplicidad **no** es la culpable a nivel de lane: `[derivado de point y se, con z=1,645 en lugar de `guardrail_inference.simultaneous_critical`=2,1573137704814935]` sólo rho75_share90 pasaría (−0,019739); rho90_share75 (−0,023304) y rho90_share90 (−0,021142) siguen fallando. Quitar la corrección entera no salva la puerta.\n\n**Experimento mínimo.** Reentrenar con `worst_product_fill` en el objetivo (Lagrangiano o CVaR sobre el producto peor servido) y confirmar sobre bloque virgen nuevo.\n\n**Falsador.** «La política con equidad en la pérdida conserva LCB95(H_OL) ≥ 0,01 y `worst_product_fill` LCB95 ≥ −0,02 en las tres celdas». *Puede fallar* en cualquiera de los dos brazos, y el fallo interesante es el primero: si al meter la equidad se pierde la adaptación, la conclusión es que la adaptación **era** la concentración.\n\n**Semillas.** **Bloque virgen nuevo obligatorio** + excepción PI documentada (patrón de `pi_exceptions` en el registro). El bloque 7490001–7490256 está `BURNED`.\n\n**Coste.** Evaluación `[derivado de A3]` ≈ **7 CPU-h medidos**. **El entrenamiento no está priceado en ningún artefacto que haya podido localizar** — no doy una cifra que no puedo reconstruir. Ordena R1 primero precisamente porque R1 responde la misma pregunta física por ~5 CPU-h y sin abrir semillas.\n\n---\n\n## 3. Lo que NO reabriría\n\n**K / K2 / K3 — muerta por física, y con el certificado puesto.** `results/k2/strong_comparators.json` bajo la física corregida (`cell.shelf_life`=156): `decomp.static_minus_mpc` = [201,9, **−1535,6**, +1964,4] — cruza cero; `decomp.static_minus_base_stock` = [−5860,6, −7400,8, −4263,5] — el base-stock es *peor* que una constante; `decomp.best_observable_minus_clairvoyant_EVPI` = [15820,2, 14754,4, 16896,2], la brecha entera es EVPI no convertible. Y `results/k3/development_screen.json` `.restricted_pi_ret_delta` = [0,0034660218253968265, 0,0006423611111111116, 0,006783699156746036] — **el techo clarividente restringido es 0,0035 contra una barra de 0,01**. Un comparador mejor no crea headroom donde el oráculo mismo se queda a un tercio de la barra. La clase ampliada la mataría más rápido, no la salvaría.\n\n**Track C — muerta por magnitud, 7,6× por debajo.** `outputs/experiments/track_c_gates_iter3_2026-07-10/c1_verdict.json` `.switcher_minus_constant_J.mean` = 2,2594235099883376e-05 con ci95 [1,3957053322321964e-05, 3,267935557448121e-05] — **limpio, 23/24 tapas positivas** — contra `.threshold_005_ret_base` = 1,7168230567775187e-04. `[derivado]` incluso el **extremo superior** del IC está 1,39e-04 por debajo de la barra. Un IC que excluye cero y está dos órdenes de magnitud bajo la materialidad no es un near-miss: es la definición de ausencia de headroom. Y el supuesto defecto de instrumento del flag C2 está refutado (el conjunto `c1.passed` se añadió en 185d52ca, 3m49s *después* de que el artefacto se escribiera, y se ve disparando en iter5).\n\n**Track B no-neuronal — muerta por comparador, y ya se re-corrió.** `results/track_b_nonneural/result.json` `.network_minus_constant.trained_mlp` = 2,176012221662134 sin ningún IC para ningún brazo de red; `results/program_n/gate_a2_track_b/result.json` `.quality_premium_over_best_nonneural` = {mean −0,5593679111583943, lcb95 −0,7476011488858794, ucb95 −0,3856069246810923, favourable 7/48} contra sesoi 0,01. `[derivado]` distancia −0,7576. **La clase ampliada ya se aplicó y el resultado empeoró en 2,7 unidades.** Volver a ampliarla no puede invertir el signo.\n\n**Program G como *lane de valor*.** R4 reabre una pregunta sobre métricas. La lane de valor está cerrada: `results/program_g/terminal_metric_audit/verdict.json` `.policy_audits.cover.paired_delta_ci95.ret_order` = [−0,02317453625750402, −0,028162880439771405, −0,018928269844804356] y `.unfulfilled_rations_at_horizon` = [+3370,2775, +2831,2709375, +3970,8655624999997]. Pierde en la métrica canónica **y** en masa de raciones no servidas. No hay comparador que arregle eso.\n\n**Program H.** `h_obs.mean` 0,0022506304678873725 con ci95 que cruza cero, favorable en 19 % de las tapas, `worst-CSSU delta −0,0385`. Ni ganó en media ni está cerca del guardarraíl. No entra.\n\n**Y una regla general, sin excepciones:** una lane que murió con `physics_no_headroom` — Track C, K2, la decisión de configuración con sus 6,5e-05 — **no revive por tener comparadores mejores ni pérdidas mejores**. Ambas capacidades nuevas actúan sobre *quién decide mejor*, no sobre *cuánto vale decidir*. Cuando `H_PI` está bajo la barra, el mejor decisor posible sigue estando bajo la barra. Es exactamente la jerarquía del CLAUDE.md: crear headroom primero.\n\n---\n\n## 4. La apuesta única\n\n**R1: Program O, la restricción de cola dentro del objetivo — precedida por R2 como filtro de 30 minutos.**\n\nCuatro razones, todas con número:\n\n1. **Es el único sitio del proyecto con premio medido.** `safe_h_pi` = 0,15151378920653932, `simultaneous_lcb95` = 0,1156159088651399 contra barra 0,01 (`contracts/program_o_full_des_hpi_translation_v1.json` → `validation_pass_rule.simultaneous_one_sided_lcb95_safe_h_pi_minimum`), con el nulo causal `exact_fungible_null_h_pi` = **0,0 exacto** y réplica en bloque disyunto (`development_custody_verdict_v1.json` `.primary.safe_h_pi` = 0,15058413468153348). `[derivado]` **2.331×** el premio de la decisión sobre la que se validó la maquinaria de Fase 3.\n\n2. **Es el único caso de victoria en media *contra el comparador fuerte*** con todas las puertas de media pasando (`gates.primary_pass`, `action_pass`, `placebo_pass`, `physical_pass` = true; 27/27 placebos batidos) y muriendo sólo en la cola. Q empató con el clásico; I y G ganaron sólo en métricas laterales. O ganó de verdad.\n\n3. **La hipótesis es específica y está medida, no inventada.** El vector de restricciones que mató a O contiene `ret_visible_cvar10` y `worst_product_fill`, y el oráculo lo satisface pagando `[derivado]` 0,00123961902735665 = **0,81 %** del headroom. La política nunca tuvo esa restricción en su objetivo. El experimento pregunta exactamente eso.\n\n4. **Cuesta menos de una noche y decide en cualquier dirección.** `[derivado de A2/A3]` ~4–8 CPU-h, cero semillas nuevas, tapas ya quemadas. Si F1 falla, la cola no es controlable y la familia O/Q se cierra **por física** — que es una frase mucho mejor para el paper que «falló un guardarraíl». Si F1 pasa y F2 falla, sabemos que la ventaja media *era* la cola. Si ambos pasan, hay por primera vez en el proyecto un candidato a conversión observable segura, y **entonces** se justifica pedir al PI la excepción de semillas vírgenes que hoy el registro prohíbe.\n\n**Condición de ejecución.** No es un rescate de Program O y no puede presentarse como tal: `independent_audit_v1.json` `.claim_boundary` marca `program_o_closed` = true y `second_rescue_forbidden` = true, y el registro marca O y Q como inmutables. Es un **programa nuevo con contrato nuevo** que hereda la física de O, se ejecuta en grado desarrollo sobre tapas quemadas, y sólo pide semillas si sus dos falsadores pasan.\n\n**Y córrelo después de R2.** Media hora de nulo de Jensen sobre `raw_calendar_matrix` decide si el 0,15151 es un premio o un artefacto del estimador. Es la prueba más barata y más letal que queda en el repositorio, y nunca se ha hecho.",
 "permission_mode": "auto",
 "prompt_id": "ee334d76-63aa-4489-9fa7-aac74d371f0b",
 "session_crons": [],
 "session_id": "fc8fefc1-10a6-4eea-bcbf-d92791120134",
 "stop_hook_active": false,
 "transcript_path": "<HOME>/.claude/projects/-Users-thom-Projects-research-scres-ia/fc8fefc1-10a6-4eea-bcbf-d92791120134.jsonl"
}
```
