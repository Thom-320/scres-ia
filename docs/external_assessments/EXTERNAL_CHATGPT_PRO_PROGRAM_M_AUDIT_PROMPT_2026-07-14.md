# External ChatGPT Pro audit prompt — Program M headroom search

Repository: `Thom-320/scres-ia`

Branch: `codex/paper2-maintenance-headroom`

Scientific snapshot to audit: `d2adb8a2bfecd76ba2f40f988bce1184f144cce0`

Audit that exact commit. It contains the metric correction, Program M contract,
full-DES implementation, complete-calendar producer, tests and VPS custody
tools, plus the fail-closed correction making the producer importable from a
clean remote clone without `PYTHONPATH`. It contains no Program M scientific
result and no opened Program M seed block.

## Role

Act as an independent senior stochastic-control, military-logistics and
simulation-verification committee. Your job is not to make RL win. Inspect the
pinned repository commit and determine whether Program M is a defensible and
high-information attempt to locate observable adaptive headroom under the
canonical thesis endpoint.

Do not rely on this prompt's numerical summaries when the repository can prove
or contradict them. Cite exact files, lines, hashes, commands and counterexamples.

## Current governing facts

1. The canonical development metric is `ret_excel_request_snapshot_v2`.
   `ret_excel_visible_v1` reconstructed backlog/lost snapshots at completion and
   is quarantined. The v2 implementation reproduces all 47,546 source workbook
   formula rows when request-time snapshots are supplied, but endogenous
   same-time event ordering remains a disclosed face-validation uncertainty.
2. No Paper 2 positive, Paper 2 family-wide null, retained-learning result or
   Paper 3 authorization currently exists.
3. The historical M/T/R VPS producer was stopped after the metric defect was
   found and produced no valid result.
4. The pasted finite R21/R3 repair-crew narrative quoting `H_PI=0.0034`,
   `0.0042` and `0.00004` has no supporting producer, raw rows, tests or verdict
   in the repository. Treat those numbers as unsupported.
5. Historical Program G found non-governing spatial perfect-information
   headroom around `0.018`, but observable policies failed under canonical ReT
   and apparent gains sacrificed worst-CSSU service. Program M is allowed to use
   this only as a mechanism pointer, never as evidence for the new contract.

## Files that must be read first

- `docs/RET_EXCEL_REQUEST_SNAPSHOT_V2_CONTRACT_2026-07-14.md`
- `research/paper2_exhaustive_search/metric_governance_audit.json`
- `research/paper2_exhaustive_search/ret_excel_request_snapshot_v2_implementation_audit_20260714.json`
- `research/paper2_exhaustive_search/phase0_failure_taxonomy.json`
- `research/paper2_exhaustive_search/approach_registry.json`
- `research/paper2_exhaustive_search/global_sensitivity_portfolio_inventory.json`
- `research/paper2_exhaustive_search/lit_comparator_obligations.md`
- `contracts/program_m_shared_lift_reservation_v1.json`
- `research/paper2_exhaustive_search/PROGRAM_M_SHARED_LIFT_PREREGISTRATION_2026-07-14.md`
- the Program M source, producer, tests and any result artifacts present at the
  pinned commit

## Program M mechanism to attack

The full terrestrial service remains in place. Once per week, before a
destination-local access-risk epoch, the operator irrevocably assigns one
protected multimodal lift slot to CSSU A or B. Every policy receives exactly
eight charged movements, 20,800 units of payload capacity and 384 committed
vehicle-hours, including empty movements. A protected shipment may bypass only the destination-local Op10
and Op12 disruption; it cannot alter production, demand, inventory, baseline
transport, CSSU-node availability or risk occurrence. All `2^8=256` open-loop
reservation calendars are admissible comparators.

The contract freezes 18 positive-hazard cells plus one unique zero-hazard cell
before results, with disjoint screen, H_PI-validation, H_obs-fit and
H_obs-validation seed roles. A result must
survive canonical ReT, quantity ReT, lost-order, worst-CSSU, backlog, tail,
mass and full transport-resource guardrails. Null physics and shuffled,
one-period-delayed and wrong-location signals are mandatory.

## Required audit questions

### A. Physical and accounting validity

- Does the implementation actually run the complete Op1-Op13 DES, or does it
  silently reduce to a Program G-style adapter?
- Does protected transport conserve every ration exactly once and preserve the
  same request-time ReT ledger semantics?
- Are reserved slots, payload capacity and vehicle-hours truly equal across
  every open-loop, feedback, oracle and learned policy? Identify any way that
  realized use, queue timing, truncation or overlapping trips buys resources.
- Is bypassing both local Op10 and Op12 outages one coherent protected trip, or
  does it accidentally create two free interventions?
- Do the 24/72/120-hour cells form a plausible disclosed envelope rather than a
  severity search chosen to make the result pass?

### B. Information and causality

- Reconstruct the exact event order: signal generation, observation, booking,
  disruption onset, shipment selection, departure, transit and delivery.
- Prove the observation whitelist excludes tape identifiers, future risk,
  future demand, oracle labels and post-action consequences.
- Verify that the real signal beats block-shuffled, delayed and wrong-location
  placebos in rollout value, not action-label accuracy.
- Verify action-ranking reversal directly and show that both A and B are
  uniquely optimal in material state fractions.

### C. Comparator completeness

- Verify enumeration of all 256 full-horizon calendars and any resource-equal
  mixtures.
- Construct the strongest implementable classical comparator: exact belief DP,
  dynamic program, stochastic MPC, max-pressure, oldest-order and robust
  reservation. Do not accept a win against constants or alternation alone.
- If full DP is not exact, provide an auditable upper bound and approximation
  gap. Determine whether a simple index rule already captures all observable
  value.

### D. Metric and guardrails

- Recompute the primary endpoint only through the repository's canonical
  request-snapshot-v2 aggregator.
- Test whether the result is caused by same-timestamp request ordering,
  completion batching, episode truncation, or lost-order denominator behavior.
- Require non-inferiority of lost orders and worst-CSSU fill. A pooled or mean
  improvement that starves one CSSU is an automatic failure.
- Check quantity-weighted ReT, service-loss AUC, maximum backlog age and tail
  outcomes for contradictions.

### E. Action-trajectory certificate

- Retain every evaluation trajectory.
- Replace each feedback policy by its modal full-horizon sequence, a
  calibration-only optimized calendar and a phase-only control.
- Measure state dependence after controlling for week. If a fixed sequence
  reproduces the policy, classify it as schedule discovery and reject adaptive
  value.

### F. Scientific decision

Return exactly one of:

1. `PROGRAM_M_READY_FOR_BURNED_TAPE_HPI_SCREEN`, listing every prerequisite that
   passed;
2. `PROGRAM_M_REQUIRES_CORRECTION`, with exact patches/tests but no result claim;
3. `PROGRAM_M_MECHANISM_INVALID`, with a counterexample or quantitative reason.

If Program M is invalid, propose at most two **materially different** mechanisms
that have a stronger first-principles chance of `H_PI >= 0.01`. Each proposal
must specify the action, scarce conserved resource, persistent consequence,
pre-action observable, exact open-loop/classical comparator, null regime and
why it does not reduce to a closed D-K3 lane. Do not propose changing the metric,
raising disruption until a learner wins, using privileged information, shedding
orders, or weakening the comparator.

Do not authorize PPO, any neural learner, virgin tapes or Paper 3. Those remain
forbidden until the complete pre-learner gate passes on burned development data.
