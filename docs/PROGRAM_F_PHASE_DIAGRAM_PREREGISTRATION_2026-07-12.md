# Program F phase-diagram screen

Status: **FROZEN BEFORE SEEDS 970001–970288**.

This screen resolves the verifier's chosen-physics objection without asking
PPO which environment it prefers. Twenty-four Latin-hypercube cells span
mitigation efficacy, signal reliability, context persistence, budget tightness,
thesis-anchored risk amplitude and commitment. Every cell is reported.

Only two-token cells are admissible for Program F v1 confirmation. One- and
three-token cells locate boundary conditions; they cannot replace the frozen
equal-budget contract. Each cell uses 12 disposable screen tapes and four
prefix-balanced states per tape. No screen tape may enter calibration, holdout
or learner evaluation.

Preselection is conjunctive: physical liveness, action diversity, material
oracle ReT headroom with positive CI, and at least 50% observable-tree
conversion with positive rollout delta. The selected cell is the first passing
cell under the frozen lowest-amplitude lexicographic order. It is never the cell
with maximum ReT or maximum conversion.

If no admissible cell passes, Program F terminates at
`STOP_PROGRAM_F_SCREEN`; seeds 940001+ remain unopened and no learner is
trained. If a cell passes, its exact parameters are frozen and re-evaluated on
fresh calibration and holdout under the stronger charter gates, including 5%
service improvement.
