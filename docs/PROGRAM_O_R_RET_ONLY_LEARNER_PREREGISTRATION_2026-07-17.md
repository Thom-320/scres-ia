# Program O-R — preregistration of the ReT-only learned-control test

Date frozen: 2026-07-17  
Machine-readable contract: `contracts/program_o_ret_only_learner_v1.json`

## Genealogy and claim boundary

Program O remains historically closed as `STOP_PROGRAM_O_AFTER_CORRECTIVE_VALIDATION`.
Its corrective validation established a positive average canonical-ReT advantage for
`belief_mpc__3` in three connected cells, with equal scheduled resources, genuine
feedback, and all 27 information placebos passed. It did not establish the frozen
joint CVaR10 safety gate in two cells.

Program O-R is a new prospective question. Canonical
`ret_excel_request_snapshot_v2` is its sole promotion endpoint because this endpoint
hierarchy was chosen before any learner was trained or any 748 scientific tape was
opened. CVaR10 and the other temporal/tail outcomes remain mandatory reported
secondaries but cannot promote or block. This does not rewrite the historical STOP.

The internal environment is a disclosed two-product researcher extension. A direct
MFSC representativeness claim remains conditional on Garrido confirming that at
least two nonfungible ration classes share Op5–Op7 capacity and on the timestamp
semantics of the ReT ledger.

## Frozen experiment

- Three cells: `rho75_share90`, `rho90_share75`, `rho90_share90`.
- Eight weekly decisions; action is the number (0–3) of the next three production
  rights allocated to `P_C`.
- Fixed-clock physical downstream resources and the Program O physics are unchanged.
- Observation is the same non-anticipative product state available to the classical
  family. Latent regime, future demand, tape identity, true cell parameters, and
  oracle calendars are prohibited.
- Intermediate reward is zero. Terminal reward is canonical ReT only.
- One frozen `RecurrentPPO` specification, ten fixed learner seeds, final checkpoint
  only, and no post-result tuning.

The complete 65,536-calendar open-loop frontier and all ten state-rich classical
configurations are evaluated on exactly the same tapes. Comparator selection is the
maximum family mean, never a per-tape clairvoyant maximum, and is repeated inside
each bootstrap resample.

The primary estimands are:

1. `H_learned = learner - max(open-loop)`;
2. `H_neural = learner - max(classical)`.

Both simultaneous lower bounds must be at least 0.01 in every cell, with at least
34/48 favorable tapes and at least 8/10 learner seeds positive against both
comparators. The action-trajectory audit must reject collapse to a fixed calendar.

Full-ledger ReT, quantity-weighted ReT, and worst-product fill use a prospectively
frozen simultaneous noninferiority margin of -0.02. Scheduled resources, mass,
partition, and demand-ledger conservation remain exact gates. CVaR10, service-loss
AUC, backlog age, losses, unresolved demand, and actual transport use are reported
without promotion authority.

Any failure produces `STOP_PROGRAM_O_R_NO_LEARNED_VALUE_OVER_CLASSICAL_FRONTIER`.
There is no reward, architecture, cell, margin, or budget rescue. Paper 3 remains
blocked unless the complete Program O-R primary gate passes.

## Execution discipline

Scientific execution is fail-closed until the contract, sources, commit, commands,
seed claim, session/watcher, and checksums are frozen. Training, comparator
calibration, and virgin confirmation use disjoint 748 namespaces. The evaluator
saves every raw open-loop matrix and every learner trajectory. A separate direct
SimPy audit replays every promoted learner and selected comparator trajectory before
an independent terminal verdict is possible.

## Relevant-risk sensitivity (separate secondary lane)

The Garrido-style relevant-risk screen is specified separately in
`docs/PROGRAM_O_R_RELEVANT_RISK_SENSITIVITY_DESIGN_2026-07-17.md`. It is not part of
the primary learner gate and cannot rescue it. It first tests only risks that touch
the shared product-mix line: R11/R14 and R21, with R22 as a downstream negative
control. R24 is deferred until a product-label and ledger rule for contingent demand
is frozen. The risk-off learner is evaluated first; any later risk fine-tuning needs
a new preregistration and cannot alter the risk-off verdict.
