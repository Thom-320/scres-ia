# Program O-R terminal calibration verdict

## Immutable status

`STOP_CALIBRATION_NOT_ELIGIBLE`

Program O-R does not change `STOP_PROGRAM_O_AFTER_CORRECTIVE_VALIDATION`, and its reserved
confirmation tapes `7480101–7480148` remain sealed.

## What calibration established

Across the three frozen cells, the ten RecurrentPPO checkpoints exceeded the complete 65,536
calendar open-loop frontier by `+0.06261` to `+0.10455` mean canonical ReT. Simultaneous lower
bounds were `+0.03659` to `+0.06630`; 41–44 of 48 tapes were favorable. Feedback, information
placebos, scheduled resources, demand preservation and 990 direct full-DES replays passed.

This is positive calibration evidence that the learner acquired state-dependent value relative to
open-loop scheduling. It is not an independent confirmation because the contract's virgin block
was never opened.

## What calibration did not establish

The learner remained `−0.00150` to `−0.00273` below the best classical configuration. It therefore
failed the frozen `H_neural >= +0.01`, favorable-tape-versus-classical and seed-versus-both gates.
No neural incremental outcome value, tail-safe deployability or retained-learning value is claimed.

The follow-up `program_q_frozen_policy_replication_v1` is a new prospective contract. It does not
reclassify this terminal STOP.
