# Program O Gate O-0 — full-action exact-transducer freeze

Status: **frozen before opening 7410001**.

This gate repairs the incomplete binary comparator used by the historical
Program O transducer.  That result remains preliminary evidence; its statement
that a full-DES implementation freeze was allowed is superseded for promotion
purposes until this gate passes.

O-0 enumerates every one of the `4^8 = 65,536` eight-week calendars.  The
weekly action is the number of three existing gross batch rights assigned to
`P_C`.  Capacity, mass, batch count, and gross resources are identical for
every calendar.  The primary mechanism is simple nonfungibility with identical
BOM, rate, processing time, and zero setup.  Cobb-Douglas, risks, unequal BOM,
partial substitution, and additional inventory are outside this contract.

The formal resource-restricted perfect-information estimand remains raw
`H_PI`.  Because the request-snapshot workbook population omits unresolved and
lost orders, promotion additionally depends on a conservative `H_PI_safe`:
for each tape, the oracle may maximize visible ReT only among calendars that
are noninferior to that tape's strongest tape-independent calendar on every
frozen service, omission, tail, product, resource, and conservation guardrail.
Both values are reported; only the safe value promotes.

The primary centered-minority sequence is attacked by both left- and
right-blocked minority schedules.  The least-favorable previously nominated
cell, `rho75_share90`, must pass under all three.  Four lower-concentration
cells (`share=0.60/0.70`) are frozen now as plausibility sensitivities and do
not enter cell selection.  Full fungibility is an exact null under all three
schedulers.

Development tapes `7410001–7410024` and validation tapes
`7410025–7410048` are new, explicitly non-virgin development universes.  The
reserved full-DES blocks `7400049–7400096` remain sealed.  Validation cannot
open until a screen result and additive validation freeze are committed.

The screen and validation stop/pass rules, raw matrix schema, action mapping,
tie rule, safe-oracle inequalities, seed blocks, and prohibited rescues are
machine-frozen in
`contracts/program_o_full_action_transducer_v1.json`.

A pass authorizes only a separately frozen product-tagged full-DES H_PI
implementation.  H_obs, learners, Paper 2 confirmation, and Paper 3 remain
prohibited.
