# Rejected local artifacts

The following untracked files appeared concurrently in the shared working tree. They are preserved as user/parallel-process work, but they are not part of this audit's evidence allowlist and must not be used to change the scientific verdict.

## Invalid bottleneck “PI ceiling”

- `bottleneck_pi_ceiling.py`
- `bottleneck_pi_ceiling.json`
- the integrated-M/T/R closure row in `paper_facing_claims_table.json`
- the downstream bottleneck-closure statements in `garrido_face_validation_questions.md`

The statistic `E[max over three constant policies] - max E[constant policy]` is a lower bound on the 24-week oracle over all admissible action calendars, not an upper bound. A weekly sequence can outperform every constant. The artifact itself acknowledges that the full weekly oracle could be higher.

It then compares the bootstrap lower confidence bound (LCB) with the 0.01 practical gate. A low LCB cannot certify that an unknown upper ceiling is below the gate; a valid closure would require a deterministic upper bound or an appropriate upper confidence bound on a proven upper-bounding random variable.

The frozen contract budgets one response-team week; M/T/R hours are allocation destinations, not three separate purchases. Unequal reserve issue/replenishment may matter, but that accounting rule was not frozen as an additional comparator budget. Neither fact closes the family. Therefore the only defensible state remains `active_for_bound`.

## Concurrent terminal-boundary commit

Commit `a91890bf` was pushed concurrently and labels the search
`SEARCH_ENVELOPE_BOUNDARY_CERTIFIED`. Its exploratory registry and stylized
Program-G/H/I atlas may be retained as inputs, but the terminal label is
superseded. The commit itself reports quantitative bounds for only at least
three of twelve families, while the requested Return B requires an explicit
ceiling for every family. Its atlas declares that it is not the full Op1--Op13
DES and does not close the still-active M/T/R full-horizon frontier.

The complete machine audit is `concurrent_boundary_commit_audit.json`. Until
its listed defects are discharged, `a91890bf` cannot authorize a boundary
return, learner training, virgin confirmation or Paper 3.

The numerical correction is stronger than a schema objection. Replaying all
64 cells reconstructs the missing seed convention and reproduces every stored
value exactly. The two cells reported above `0.01` came from the full-order
formula and both fail lost-order non-inferiority with CI95 entirely positive.
Direct visible-ledger optimization selects `HOLD^4`: it loses all 48 orders,
emits no visible row and receives the empty-population value 1.0. Its apparent
64/64 zero is an unguarded sparse-ledger shed-to-win degeneracy, not a project
H_PI ceiling. A paired 0-versus-0.30 R22
test changes the R22 tapes but changes every scored sequence by exactly zero.
The versioned evidence is
`results/paper2_search/voi_ceiling_atlas_corrective_audit.json`.
