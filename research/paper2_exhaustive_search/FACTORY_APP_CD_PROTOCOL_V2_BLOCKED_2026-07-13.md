# Factory-APP Cobb-Douglas protocol v2 — blocked before preregistration

Date: 2026-07-13
Status: `BLOCKED_BEFORE_PREREGISTRATION__V1_UNAUTHORIZED_TAPES_QUARANTINED`

This replaces, rather than silently amends, the rejected v1 preregistration
identified by `factory_app_cd_gate_v1_rejection.json`. A later provenance audit
found an unauthorized v1 JSON consuming `9,600,001–9,600,400`. Those 400 tapes
are permanently burned and the output is quarantined because the rejected
physics, privileged selector, incomplete frontier and absent custody chain make
its numbers non-evidence. No valid policy result exists. A v2 protocol must use
a new disjoint seed block.

## Claim boundary

`supply_chain.factory_resilience` may compute the published Garrido et al. 2024
factory-APP transformation from valid positive components. It does not validate
an MFSC mapping, an APP simulator, an adaptive policy or a Paper 2 endpoint.
For the current Paper 2 contract, `ret_excel_visible_v1` remains primary and
Cobb-Douglas remains secondary construct sensitivity. A CD ranking cannot
promote a canonical-ReT null or a service/fairness failure.

## Facts required before a real preregistration

1. Obtain the paper's actual 36-period demand seed or explicitly label and
   source a new demand process and its parameter envelope.
2. Resolve the published handling of zero inventory, backorders and spare
   capacity before applying logarithms; no post-result continuity constant is
   allowed.
3. Reconstruct Algorithms 1 and 2 with hand-calculated fixtures for S11-S32,
   including cumulative-prefix workforce rules, constant level production,
   terminal-period exceptions, overtime and the published cost ledger.
4. Freeze cost normalization at the same experiment/strategy level used by the
   paper. A policy score may not depend on unavailable per-tape counterfactual
   future outcomes.
5. Choose one honest decision contract:
   - episode-level contextual selection using only a pre-commitment historical
     estimate such as `cv_hat`; report its oracle as `EVPI_7pure`, not H_PI; or
   - a new per-period switcher with sourced switching cost, activation delay,
     minimum dwell and conservation rules.
6. Freeze a componentwise resource frontier. The comparator set must include
   every fixed pure strategy, every feasible full-horizon open-loop schedule or
   a certified bound, and the strongest APP DP/MPC/hysteresis policies.
7. Freeze calibration, validation and one-time confirmation blocks, tape hashes,
   selection-adjusted inference, practical effect, adjacent cells, mechanism-
   removal null, shuffled/delayed placebos and action-trajectory replacements.
8. Before MFSC embedding, validate hiring, firing, overtime, inventory,
   backorder, capacity and cost ledgers with units and conservation tests and a
   Garrido sign-off question. Then require co-primary canonical ReT plus lost-
   order, worst-node, quantity-ReT, tail-risk and resource non-inferiority.

Until all eight items are frozen in a new versioned contract, no APP tape may
be opened and no learner may be trained. Existing repository evidence remains
the governing headroom result: the adapted-CD exploratory spread was about
`0.00816` (not certified H_PI and below the 0.01 gate), while PPO lost to the
best static CD policy in the current, increased and severe regimes.
