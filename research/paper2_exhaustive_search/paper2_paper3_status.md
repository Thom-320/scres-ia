# Paper 2 preregistration / verdict and Paper 3 authorization status

Date: 2026-07-13

## Paper 2

No new candidate from this exhaustive-search round is eligible for a Paper 2 confirmatory preregistration.

The last implemented candidate is the integrated M/T/R bottleneck-migration contract. Its preregistered observable gate stopped on already-opened locked tapes:

- canonical ReT delta: `-0.001309`, CI95 `[-0.006384,+0.003093]`;
- service-loss reduction: `-0.03035`, CI95 `[-0.03877,-0.02246]`;
- favorable tapes: `53.3%`;
- lost orders: non-inferior;
- scalar total team-hours, CRN and mass checks: pass;
- PPO: not trained;
- learner range `1120001+`: unopened.

The corrective comparator/resource audit now shows that the original gate was not a family exhaustion:

- 11,184,811 effective full-horizon calendars exist;
- only three constants were compared;
- total one-team hours are equal by construction; M/T/R hours are allocation destinations, while reserve issue/replenishment semantics still require a frozen ledger interpretation;
- there is no resource-restricted PI ceiling;
- the null cell is implementable but was not frozen prospectively.

Therefore the effective status is:

`NO_PAPER2_RESULT__ACTIVE_BOUND_AND_DOMAIN_FACTS_REQUIRED`.

Program L does not change this status. Its full-DES development screen used the canonical aggregator, but its purported `H_PI` was a myopic true-state rule, its comparator frontier was incomplete, and its two positive-mean cells used more departures with LCB95 below zero. The tested heuristic contract is a no-go; thesis-native alternate-route value is exactly zero because the action is absent; the introduced route family remains blocked on Garrido facts and a certified bound.

No seed block may be renamed virgin. In particular, `6800001+`, `6900001+`,
`1100001–1100060`, `1110001–1110120`, and the unauthorized APP/CD range
`9600001–9600400` are burned for the purposes for which they were opened.
The rejected APP/CD declaration from `9600401+` is withdrawn rather than a
reusable virgin reservation; any valid future APP protocol must allocate a
wholly disjoint block. Learner range `1120001+` remains unopened/reserved.
Commit `a91890bf` also opened a 64-cell stylized atlas with reconstructed blocks
`7200001 + 1000*cell_index` through `+47`; the entire `7200001-7299999`
region is conservatively excluded by the corrected burn ledger.

The retrospective full-horizon M/T/R computation is frozen in
`contracts/paper2_bottleneck_full_horizon_bound_v1.json`. Tape `1110001` was
used to develop the acceleration and is excluded from the 119-tape locked
inference. The acceleration must pass exhaustive reduced-horizon equivalence
and selected-winner full replays before it can change the family state.

The first key-v3 execution attempt at commit `179f7c2` is explicitly invalid:
an adversarial review found that Markov-state completeness was asserted rather
than machine-certified, exact 60/119 tape-certificate identities were not
reconciled across every manifest, and execution receipts were checksum-protected
without semantic reconciliation. W12, W16 and W24 processes were intentionally
stopped before result. They are diagnostics only and authorize nothing; see
`mtr_execution_invalidation_179f7c2.json`.

The successor proof is identified separately as semantic key v4, exact-result
schema v5 and bound-authorization schema v9. Its complete proof, frontier and
signed-custody test suites pass on development bytes, including independent
attacks on real W2 child launches and portable two-transfer-chain archive
reverification. Those W2 probes were deliberately non-scientific and correctly
rejected as evidence. Until the schema-bumped bytes are committed immutably and
new separately keyed W12 and W16 scientific pairs pass, the governing status is
`OPEN_ACTIVE_BOUND_REQUIRED__MTR_KEY_V4_SIGNED_REDUCED_GATE_PENDING`; W24,
`H_PI`, Paper 2 learning and Paper 3 remain unauthorized.

Risk editing does not currently authorize a numeric rescue. The thesis-supported
current-to-increased R11–R24/R3 cells change occurrence only and add no
pre-action observation or scarce decision right. The highest-priority open
domain question is R21/R3 Maintenance Battalion restoration sequencing:
current independent parallel recovery gives exact zero action liveness, while
a scarce-team extension is `blocked_domain_fact`, not falsified, because the
historical restoration-order-invariance assertion lacks a machine proof. See
`r21_r3_restoration_registry_correction.json` and Garrido Q7.

The independent Op3-Op13 census also corrected an overbroad catalog-coverage
claim. All 32 rights declared in `decision_right_catalog_v1` were routed, but
that declaration omitted component-specific R13/Op4 kit balancing and an
observed R14 lot-disposition right, and it mislabeled Op7 inspection as Op6.
All three increments have exact current-kernel `H_PI=H_obs=0` because the
actions/state are absent (and current same-rate unit-yield rework weakly
dominates discard). They are `blocked_domain_fact`, not global impossibility
results; see `inspection_quality_and_decision_gap_correction.json` and Garrido
Q8-Q10. Cobb-Douglas cannot make an absent transition live and remains a
secondary construct-sensitivity metric.

## Pre-learner freeze if a domain-blocked family reopens

Before any result, freeze in one versioned contract:

- sourced physics and intervention ledger;
- observation whitelist and timing;
- action cadence/activation/dwell;
- componentwise resources and conservation equations;
- canonical metric and all guardrails;
- complete comparator family and certified gaps;
- development, calibration, validation and virgin seed ranges;
- action-support/practical-effect/null/placebo gates;
- fixed-sequence and phase replacements;
- a single learner architecture/budget/seeds and stop rule.

No current family clears this entry gate.

## Paper 3

Paper 3 is prohibited. `H_learned > 0` has not been established under the current strongest comparator definition.

The transient K3 Paper-3 authorization is superseded by the corrective period-8 audit. No retained-learning result, campaign preregistration, or new retention run may be promoted while Paper 2 is false.

The only scientifically valid Paper-3 statement is:

`PAPER3_NOT_AUTHORIZED__DEPENDENCY_PAPER2_LEARNED_VALUE_UNMET`.
