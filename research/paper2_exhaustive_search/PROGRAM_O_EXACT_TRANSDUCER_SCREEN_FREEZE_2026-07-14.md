# Program O exact-transducer screen freeze

Frozen after the workbook affected-order gate passed and before seeds
`7400001–7400024` were opened.

This stage repairs the decisive defects of the historical F8 toy: it scores
with `ret_excel_request_snapshot_v2`, serves backlog from later inventory,
freezes the complete weekly product commitment, enumerates every one of the
`2^8=256` full-horizon calendars, preserves total demand and production, and
requires an exact fully-fungible null.

The binary action is deliberately a small falsification contract: each week is
either two C batches plus one H batch or the reverse. The full four-action
`k in {0,1,2,3}` frontier remains mandatory in the later full-DES contract; a
positive here cannot bypass that stronger comparator.

The screen opens only 24 development seeds. It promotes no cell directly. A
screen pass requires at least two adjacent cells with mean exact
oracle-minus-best-static H_PI at least 0.015, exact null identity, non-superior
oracle unfulfilled quantity and no worse worst-product fill. A later validation
rule must be frozen before seeds 7400025+ are opened.

No H_obs policy, learner, Paper 2 confirmation or Paper 3 work is authorized.
