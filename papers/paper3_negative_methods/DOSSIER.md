# P3 dossier — When Not to Train

Status: `HOLD`. No new executions are authorized while Paper 2 is released.

Candidate thesis: under the thesis-grounded reconstructed DES and the repaired endpoint contract,
adaptive headroom may be operationally negligible even when the best posture changes across risk
profiles.

Before any manuscript is opened, the dossier must pass three gates:

1. endpoint crosswalk from `ret_excel_request_snapshot_v2` to the repaired multi-endpoint evidence;
2. domain sign-off on Program O ledger semantics, mission expiry/triage and a genuinely scarce shared
   resource;
3. a new `P3_NEGATIVE_METHODS` identity and explicit PI authorization that the historical exhaustion
   certificate may be reused as a negative-methods paper.

The existing risk screen supports only the profile-level statement: the optimum moves among at most
three postures, but following it buys (6.93\times10^{-5},[0,2.08\times10^{-4}]) against a 0.01
bar. It does not answer within-episode stochastic realization.
